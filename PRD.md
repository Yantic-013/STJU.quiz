# 刷题助手前端产品需求文档

> **当前实施范围（V1.1）**：产品仅保留单选题和填空题；多选题配置、统计和导入已停用。自定义示例章节“第一章 绪论”“第二章 基础”不再保留。以下历史原型中的多选题描述仅作为早期设计记录，不属于当前上线范围。

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 刷题助手 |
| 文档版本 | V1.0 |
| 文档状态 | 前端 UI 重构需求稿 |
| 产品形态 | 纯前端、本地数据存储、支持离线使用的 Web 刷题应用 |
| 主要终端 | 桌面浏览器、平板浏览器、手机浏览器、PWA 添加到主屏幕后使用 |
| 原型用途 | 作为 Stitch 页面原型和交互流程设计的功能依据 |
| 本期重点 | 完成现有功能的信息架构、页面模块、交互状态和移动端结构重构，不新增后端服务 |

## 2. 文档背景

当前项目已经具备完整的本地刷题能力，包括内置题库、Excel 题库导入、章节练习、模拟考试、错题集、收藏集、全文搜索、题图关联、数据备份、深浅主题和离线缓存。

本次重构不改变题库数据格式、判分逻辑和本地存储方式，重点解决以下前端产品问题：

1. 梳理页面层级和任务路径，使用户可以更快开始练习。
2. 统一练习、考试、错题重练和收藏重练的答题体验。
3. 明确题目、进度、答案、标记、收藏和判分状态。
4. 补齐空状态、加载状态、错误状态和操作反馈。
5. 提升手机和平板上的可用性。
6. 为后续使用 Stitch 生成原型图提供完整、无歧义的页面需求。

## 3. 产品目标

### 3.1 核心目标

- 用户进入应用后，可以在两步内开始一次章节练习。
- 用户能够按章节和题目范围组织练习。
- 用户能够从多个章节随机抽题并生成模拟考试。
- 用户能够在一次答题过程中清楚掌握当前进度、已答题、未答题和标记题。
- 用户交卷后能够快速定位错题、理解错误原因并再次练习。
- 用户能够独立管理本地题库、题图和数据备份。
- 核心刷题能力在离线环境下仍可使用。

### 3.2 体验目标

- 降低首次使用和首次导入题库的理解成本。
- 降低长时间连续答题时的认知负担。
- 降低误交卷、误删除、误清空数据的风险。
- 确保桌面端、平板端和手机端的主要任务路径一致。
- 所有关键操作均有明确的结果反馈。

### 3.3 非目标

以下能力不属于本期范围：

- 用户注册、登录和多账号体系。
- 云端同步、跨设备同步和服务器备份。
- 教师端、班级管理、作业下发和排行榜。
- 在线支付、会员和商业化能力。
- AI 自动出题、AI 解析和智能学习计划。
- 题库在线市场或多人协作编辑。
- 修改现有 Excel 解析规则和核心判分规则。

## 4. 目标用户

### 4.1 核心用户：课程学习者或备考者

典型特征：

- 使用机械设计、机械原理等课程题库复习。
- 需要按章节逐步学习，也需要考前随机组卷。
- 一次可能连续完成几十道题。
- 需要反复查看错题、正确答案、解析和题图。
- 可能在电脑上管理题库，在手机或平板上刷题。

核心诉求：

- 快速找到要练习的章节。
- 自由设置本次练习范围。
- 清楚了解当前答题进度。
- 交卷后立即知道成绩和错误原因。
- 集中处理错题和重点收藏题。

### 4.2 次要用户：题库维护者

典型特征：

- 使用 Excel 整理题目。
- 需要增量更新题库和批量关联题图。
- 关注导入是否成功、哪些题目被更新或跳过。
- 需要定期导出完整备份。

核心诉求：

- 导入规则容易理解。
- 导入过程和结果透明。
- 数据清理操作安全。
- 备份文件完整且易于恢复。

## 5. 产品约束与基本规则

### 5.1 技术与数据约束

- 产品为纯静态前端应用，不依赖自有后端。
- 题库、错题、收藏、设置和本地题图保存在当前浏览器本地。
- 用户清理浏览器数据、更换设备或更换浏览器后，本地数据可能丢失。
- 应通过完整备份导出能力降低数据丢失风险。
- 已完成离线缓存后，用户应能继续浏览题库和完成练习。
- Excel 解析和本地数据库依赖加载失败时，需要展示明确的降级说明。

### 5.2 支持的题型

| 题型 | 作答方式 | 判分规则概述 |
|---|---|---|
| 单选题 | 从多个选项中选择一个 | 用户答案与正确选项一致即为正确 |
| 多选题 | 从多个选项中选择一个或多个 | 所有正确选项完全一致才判为正确 |
| 填空题 | 根据空位数量输入一个或多个答案 | 忽略空格、常见标点和大小写；支持同一空的多个可接受答案 |

多空填空题支持两种答案顺序：

- 固定顺序：用户必须按题目空位顺序填写。
- 顺序可交换：各空答案内容正确即可，不要求顺序一致。

### 5.3 题目顺序与选项规则

- 每次开始练习或考试时，题目顺序随机排列。
- 选择题选项可以随机排列。
- 选项随机排列后，判分必须继续对应原始正确答案。
- 填空题不显示选择题选项。
- 题图在答题、结果、错题、收藏和搜索场景中均应可查看。

### 5.4 错题与收藏规则

- 用户答错或未作答的题目在交卷后加入错题集。
- 同一道题再次答错时，累计错误次数并更新最近答错日期。
- 收藏由用户主动添加或取消，并跨练习保存。
- 当前答题过程中的“标记题”仅用于本次检查，不等同于收藏。
- 题目从题库移除后，与之关联的无效错题和收藏记录应被清理或忽略。

## 6. 核心使用场景

### 6.1 章节练习

1. 用户进入“章节练习”。
2. 查看资料分类、章节名称及各题型数量。
3. 点击目标章节。
4. 设置起始题号和结束题号，或选择快捷范围。
5. 开始练习。
6. 完成作答并交卷。
7. 查看正确率和错题详情。

### 6.2 模拟考试

1. 用户进入“模拟考试”。
2. 选择一个或多个章节。
3. 分别设置单选、多选和填空题数量。
4. 查看选中章节中的可用题量。
5. 生成随机试卷并开始作答。
6. 交卷后查看百分制得分、正确率和错题。

### 6.3 错题复习

1. 用户进入“错题集”。
2. 按章节筛选或查看全部错题。
3. 查看错误次数、最近答错日期、答案和解析。
4. 选择重新练习当前筛选结果或全部错题。
5. 对已经掌握的题目执行移除操作。

### 6.4 收藏复习

1. 用户在答题页或搜索页收藏题目。
2. 进入“收藏集”。
3. 按章节查看收藏题。
4. 查看答案和解析，或重新练习收藏题。
5. 取消不再需要的收藏。

### 6.5 搜索题目

1. 用户进入“题目搜索”。
2. 输入题干、章节、选项、答案或解析中的关键词。
3. 查看匹配数量和关键词高亮结果。
4. 查看题目详情或收藏目标题目。

### 6.6 题库维护

1. 用户进入“题库管理”。
2. 查看总题数、各题型数量、题图数量和错题数量。
3. 上传或拖入 Excel 文件。
4. 查看解析进度和导入结果。
5. 根据题目 ID 批量上传题图。
6. 导出完整备份或错题数据。
7. 必要时清理自定义题库或错题集。

## 7. 信息架构

### 7.1 一级导航

桌面端一级导航包括：

1. 章节练习
2. 模拟考试
3. 错题集
4. 收藏集
5. 题目搜索
6. 题库管理

全局辅助操作：

- 浅色/深色主题切换。
- 当前设备本地数据状态说明。
- 移动端“更多”入口。

### 7.2 流程页面

以下页面不作为一级导航入口，而由具体任务进入：

- 访问验证页。
- 练习配置弹层。
- 答题页。
- 答题结果页。
- 确认对话框。
- 题图预览层。

### 7.3 页面跳转关系

- 访问验证页 → 章节练习页。
- 章节练习页 → 练习配置 → 答题页 → 答题结果页。
- 模拟考试页 → 答题页 → 答题结果页。
- 错题集页 → 答题页 → 答题结果页。
- 收藏集页 → 答题页 → 答题结果页。
- 任意无题库页面 → 题库管理页。
- 答题结果页 → 章节练习页，或再次练习。

## 8. 页面详细需求

## 8.1 访问验证页

### 页面目标

阻止未验证用户直接进入应用，并在当前设备上记住验证结果。

### 模块结构

1. 产品名称与简短说明。
2. 访问口令输入框。
3. 显示/隐藏口令操作。
4. 进入应用按钮。
5. 错误信息区域。
6. “本设备验证一次后会自动记住”的说明。

### 功能规则

- 未验证时不能查看主导航和主要页面内容。
- 输入正确口令后进入应用默认页。
- 当前设备已经验证过时，后续访问直接进入应用。
- 口令为空时不允许提交。
- 口令错误时在输入框附近显示错误信息。
- 支持键盘回车提交。

### 页面状态

- 默认状态：输入框为空，主按钮可根据输入状态启用。
- 验证中：按钮显示处理中并防止重复提交。
- 验证失败：显示明确错误，焦点返回输入框。
- 验证成功：进入应用并隐藏验证页面。

## 8.2 章节练习页

### 页面目标

帮助用户从资料分类和章节结构中快速选择一次练习的内容。

### 模块结构

1. 页面标题和功能说明。
2. 题库总题数。
3. 资料一级分组列表。
4. 分组展开/收起控制。
5. 章节列表。
6. 每章题型数量：单选、多选、填空。
7. 无题库引导模块。

### 功能规则

- 章节按资料或课程名称进行一级分组。
- 分组内显示具体章节。
- 默认可展开全部分组；用户可以逐组收起。
- 点击章节后打开练习配置。
- 章节中没有题目时不应提供开始入口。
- 页面总题数应与当前本地题库保持一致。

### 原型需要表达的状态

- 正常列表状态。
- 一级分组展开状态。
- 一级分组收起状态。
- 章节点击或聚焦状态。
- 整个题库为空状态。

## 8.3 练习配置弹层

### 页面目标

让用户在开始章节练习前选择连续题号范围。

### 模块结构

1. 弹层标题。
2. 当前章节名称。
3. 章节总题数。
4. 起始题号输入框。
5. 结束题号输入框。
6. 本次选中题数实时汇总。
7. 每 50 题一组的快捷范围按钮。
8. 全部题目快捷按钮。
9. 取消按钮。
10. 开始练习按钮。

### 功能规则

- 起始题号和结束题号必须为整数。
- 起始题号不能小于 1。
- 结束题号不能大于章节总题数。
- 结束题号不能小于起始题号。
- 输入变化后实时更新本次题数。
- 范围无效时，显示字段级错误并禁用开始按钮。
- 点击快捷范围后同步更新两个输入框。
- 点击遮罩、取消按钮或键盘 Escape 可关闭弹层。
- 桌面端使用居中对话框；手机端使用底部面板。

## 8.4 模拟考试配置页

### 页面目标

让用户从多个章节中按题型数量生成一套随机模拟试卷。

### 模块结构

1. 页面标题和考试说明。
2. 章节多选区域。
3. 每章单选、多选、填空数量。
4. 全选和取消全选。
5. 单选题数量输入。
6. 多选题数量输入。
7. 填空题数量输入。
8. 当前题库总量说明。
9. 已选章节可用题量汇总。
10. 本次考试总题数。
11. 总分和计分规则说明。
12. 开始考试按钮。

### 功能规则

- 默认选中全部章节。
- 默认题数为：单选最多 10 题、多选最多 5 题、填空最多 5 题，但不能超过实际题量。
- 每种题型允许设置为 0，但三种题型不能同时为 0。
- 至少选择一个章节。
- 章节选择变化后，实时更新各题型可用题量。
- 用户输入题数不能超过已选章节中的可用题量。
- 配置不合法时，开始按钮不可用，并显示具体原因。
- 试卷总分为 100 分，所有题目按题等权计分。
- 点击开始后，从已选章节内按题型随机抽题，再打乱整张试卷顺序。

### 原型需要表达的状态

- 全部章节已选。
- 部分章节已选。
- 未选择章节。
- 某题型题量超过可用数量。
- 三种题型数量均为零。
- 无题库状态。

## 8.5 答题页

### 页面目标

支持用户高效、连续地完成章节练习、模拟考试、错题重练或收藏练习。

### 模块结构

1. 答题上下文：练习名称或考试名称。
2. 当前题型标识。
3. 当前题号、总题数、已答题数。
4. 总体答题进度。
5. 题号导航。
6. 当前题干。
7. 题图区域。
8. 作答区域。
9. 本题标记操作。
10. 收藏操作。
11. 上一题、下一题或交卷操作。
12. 当前题型操作提示。

### 题号导航规则

题号至少包含以下状态：

- 当前题。
- 已作答。
- 未作答。
- 已标记。
- 当前且已作答。
- 当前且已标记。

交互要求：

- 点击任意题号可直接切换到对应题目。
- 支持收起和展开题号导航。
- 题号较多时支持滚动，并确保当前题号可见。
- 手机端题号导航放入底部抽屉或独立面板。

### 单选题规则

- 点击一个选项后，取消其他选项并记录答案。
- 当前实现支持选中后自动进入下一题；重构时需要保留该能力。
- 最后一题选中后不自动交卷。
- 已选选项需要保持明显状态。
- 用户返回该题时恢复之前的选择。

### 多选题规则

- 点击选项可切换选中或取消。
- 支持选择一个或多个选项。
- 页面明确提示“可多选”。
- 用户返回该题时恢复全部选择。

### 填空题规则

- 根据题目空位数量展示相同数量的输入框。
- 多空题显示“第 1 空、第 2 空”等标签。
- 答案顺序可交换时显示明确提示。
- 页面说明判分会忽略空格、常见标点和大小写。
- 切换题目时保存已输入内容。
- 手机软键盘弹出时，当前输入框不能被固定操作栏遮挡。

### 标记与收藏规则

- “标记”用于本次答题过程中稍后检查。
- “收藏”长期保存在本地，并同步到收藏集。
- 两者在名称、图标和状态反馈上必须明确区分。
- 收藏成功或取消后提供即时反馈。

### 翻页与交卷规则

- 第一题的“上一题”不可用。
- 非最后一题显示“下一题”。
- 最后一题显示“交卷”。
- 下一题允许在当前题未作答时使用。
- 交卷前显示确认信息，包括总题数、未答题数和标记题数。
- 未答题交卷后按错误处理。
- 用户尝试退出正在进行的答题流程时，提示当前进度可能丢失。

### 题图规则

- 有题图时在题干和作答区域之间展示。
- 保持原始比例，不裁剪、不拉伸。
- 图片超出可视区域时自适应缩放。
- 点击图片可进入大图预览。
- 图片加载失败时显示题图不可用提示，但不影响继续答题。

## 8.6 答题结果页

### 页面目标

让用户快速了解本次表现，重点查看错误题目并进入下一轮练习。

### 模块结构

1. 本次练习或考试名称。
2. 结果摘要。
3. 总题数。
4. 正确题数。
5. 错误题数。
6. 正确率。
7. 模拟考试百分制得分。
8. 每题分值说明。
9. 错题详情列表。
10. 返回章节列表。
11. 再练一次或重练错题入口。

### 错题详情内容

- 原题序号。
- 题型。
- 题干。
- 答题中是否被标记。
- 用户答案。
- 正确答案或填空题参考答案。
- 选项及正确选项状态。
- 题图。
- 题目解析。

### 功能规则

- 结果页优先展示错题，不要求默认展开所有正确题。
- 错题详情默认收起，点击后展开。
- 未作答明确显示“未作答”。
- 模拟考试按总分 100 分计算，展示得分和每题分值。
- 全部答对时显示成功状态，不显示空的错题列表。
- 所有错误题在交卷时写入错题集。
- 返回章节列表后结束本次答题状态。

### 原型需要表达的状态

- 存在错题的练习结果。
- 全部答对的练习结果。
- 模拟考试结果。
- 错题详情收起和展开。
- 用户未作答的题目。

## 8.7 错题集页

### 页面目标

帮助用户集中查看、筛选和重新练习历史错题。

### 模块结构

1. 页面标题与错题总数。
2. 章节筛选器。
3. 重新练习操作。
4. 错题列表。
5. 每题的错误次数和最近答错日期。
6. 正确答案或参考答案。
7. 题图和解析详情。
8. 移出错题集操作。

### 功能规则

- 默认显示全部章节的错题。
- 章节筛选后只显示对应错题。
- “重新练习”默认针对当前筛选结果，并显示题目数量。
- 点击错题列表项可展开或收起详情。
- 移出错题集前需要确认，或在移除后提供撤销。
- 无法在题库中找到原题的无效记录不展示。
- 当前筛选结果为空时，不影响用户切换回全部章节。

### 建议排序

本期默认使用最近答错时间倒序；同时在产品结构中为以下排序预留入口：

- 最近答错。
- 错误次数最多。
- 章节顺序。

## 8.8 收藏集页

### 页面目标

帮助用户集中管理和练习主动收藏的重点题目。

### 模块结构

1. 页面标题与收藏总数。
2. 章节筛选器。
3. 重新练习操作。
4. 收藏题列表。
5. 正确答案或参考答案。
6. 题图和解析详情。
7. 取消收藏操作。

### 功能规则

- 默认显示全部收藏。
- 章节筛选后只显示对应收藏题。
- “重新练习”针对当前筛选结果，并显示题目数量。
- 点击题目可展开或收起详情。
- 取消收藏后列表立即更新，并提供短时撤销。
- 收藏状态需与答题页和搜索页同步。

## 8.9 题目搜索页

### 页面目标

帮助用户通过关键词快速定位题目并查看完整参考信息。

### 模块结构

1. 搜索输入框。
2. 清除输入操作。
3. 搜索范围提示。
4. 题库总题数或匹配数量。
5. 搜索结果列表。
6. 结果中的题型、章节、题干、题图、选项、答案和解析。
7. 收藏操作。

### 搜索范围

关键词需要匹配：

- 题干。
- 章节名称。
- 选择题选项。
- 正确答案或填空参考答案。
- 题目解析。

### 功能规则

- 输入变化后约 200 毫秒执行搜索。
- 搜索忽略英文字母大小写。
- 显示匹配题目数量。
- 对题干、选项和解析中的关键词进行高亮。
- 用户可以直接在结果中收藏或取消收藏。
- 清除按钮清空关键词、结果并重新聚焦输入框。
- 大量结果应分批渲染或分页，避免页面卡顿。

### 页面状态

- 未输入关键词。
- 正在搜索。
- 存在结果。
- 无搜索结果。
- 题库为空。

## 8.10 题库管理页

### 页面目标

集中完成题库导入、题图关联、数据统计、备份和数据清理。

### 模块结构

1. 题库概览。
2. Excel 导入。
3. 导入规则说明。
4. 题图批量关联。
5. 数据导出。
6. 题库章节总览。
7. 危险操作区域。

### 题库概览指标

- 总题数。
- 单选题数。
- 多选题数。
- 填空题数。
- 已关联题图数量。
- 有效错题数量。

### Excel 导入模块

功能要求：

- 支持点击选择文件和拖拽文件。
- 仅接受 `.xlsx` 文件。
- 上传前展示文件名称和文件大小。
- 提供题库模板下载或模板位置说明。
- 展示处理阶段：读取文件、识别表头、解析题目、合并数据、保存完成。
- 处理期间禁止重复上传。
- 导入完成后展示结果摘要。

导入结果至少包括：

- 新增题目数量。
- 更新题目数量。
- 跳过重复题目数量。
- 格式无效题目数量。

导入规则：

- 自动查找包含“章节、题型、题目、正确答案”的真实表头行。
- 题目 ID 存在时优先按 ID 更新。
- 没有题目 ID 时，按“章节 + 题干”匹配。
- 匹配到已有题目时更新，未匹配时新增。
- 坏行不阻断整个文件导入，但必须计入无效数量。
- 选择题支持 A 至 Z 的扩展选项列。
- 支持题目 ID、资料名称、考点、原书页码、局部题号、全局题号等可选字段。
- 支持题图、图片、图片路径、题图文件等图片字段名称。

### 题图批量关联模块

功能要求：

- 支持一次选择多张图片。
- 支持 PNG、JPEG、WebP 和 SVG。
- 图片文件名需与题目 ID 一致。
- 忽略扩展名后进行题目 ID 匹配。
- 完成后显示成功关联数量和未匹配文件名。
- 新上传的同 ID 图片覆盖旧图片前应有明确提示。

### 数据导出模块

完整备份应包含：

- 题库。
- 错题记录。
- 收藏记录。
- 用户设置。
- 本地题图。
- 数据版本和导出时间。

另提供错题集 JSON 独立导出。

### 题库章节总览

每个章节显示：

- 章节名称。
- 单选题数。
- 多选题数。
- 填空题数。
- 章节总题数。

### 危险操作

支持：

- 清空用户导入的自定义题库。
- 清空错题集。

规则：

- 危险操作与普通导入、导出操作明确分区。
- 必须使用产品内确认对话框。
- 确认信息需要说明将删除什么、将保留什么、是否可以恢复。
- 存在内置默认题库时，清空自定义题库后保留默认题库。
- 清空题库后同步清理与已删除题目关联的错题、收藏和本地题图。
- 操作成功后展示完成反馈。

## 9. 全局组件需求

### 9.1 导航

- 清晰显示当前页面。
- 桌面端保持六个一级入口。
- 移动端对入口进行收纳，避免横向滚动导航。
- 答题过程中使用独立的答题导航，减少误退出。

### 9.2 按钮

所有按钮需要具备：

- 默认状态。
- 聚焦状态。
- 按下状态。
- 禁用状态。
- 加载状态。
- 成功或失败后的反馈状态。

按钮层级包括：

- 主要操作。
- 次要操作。
- 文字或轻量操作。
- 危险操作。

### 9.3 表单控件

- 标签不能仅依赖输入框占位文字。
- 错误信息在对应字段附近显示。
- 数字输入需要显示合法范围。
- 多选章节、选项和题号状态不能只靠颜色区分。
- 键盘焦点清晰可见。

### 9.4 对话框与底部面板

适用场景：

- 练习范围配置。
- 交卷确认。
- 清空数据确认。
- 移出错题确认。
- 题图预览。

要求：

- 打开后焦点进入弹层。
- 关闭后焦点返回触发元素。
- 支持 Escape 关闭非强制弹层。
- 危险确认的主操作必须明确写出动作，例如“清空错题集”，不能只写“确定”。

### 9.5 操作反馈

适合短时反馈的操作：

- 收藏成功或取消收藏。
- 移除错题。
- 数据保存成功。
- 导出已开始。
- 题图关联完成。

适合持续显示的反馈：

- 文件解析失败。
- 本地存储失败。
- 外部依赖不可用。
- 题库格式不正确。

## 10. 空状态需求

| 页面或场景 | 标题 | 说明 | 主操作 |
|---|---|---|---|
| 章节练习无题库 | 暂无题库内容 | 需要先导入题库后才能开始练习 | 去题库管理 |
| 模拟考试无题库 | 暂无可用题目 | 导入题库后才能配置模拟考试 | 去题库管理 |
| 错题集完全为空 | 错题集为空 | 答错或未作答的题目会自动收录 | 开始章节练习 |
| 当前章节无错题 | 当前章节暂无错题 | 可以切换章节或查看全部错题 | 查看全部章节 |
| 收藏集完全为空 | 收藏集为空 | 可在答题页或搜索结果中收藏题目 | 去章节练习 |
| 当前章节无收藏 | 当前章节暂无收藏 | 可以切换章节或查看全部收藏 | 查看全部收藏 |
| 搜索未输入 | 搜索题库 | 可搜索题干、章节、选项、答案和解析 | 聚焦搜索框 |
| 搜索无结果 | 未找到相关题目 | 展示当前关键词，并建议更换或缩短关键词 | 清除关键词 |
| 结果页无错题 | 本轮全部答对 | 本轮没有需要订正的题目 | 继续练习 |
| 题图未关联 | 暂无已关联题图 | 图片文件名需要与题目 ID 一致 | 选择题图 |

空状态原则：

- 说明为什么为空。
- 告诉用户内容将如何产生。
- 提供一个最合理的下一步操作。
- 筛选无结果与数据完全为空必须使用不同状态。

## 11. 加载与处理中状态

### 11.1 应用启动

- 加载本地题库、错题、收藏、设置和题图时展示页面骨架。
- 首次载入内置默认题库时显示“正在准备题库”。
- 不应先短暂显示空状态，再跳转为正常内容。
- 加载失败时提供重试入口。

### 11.2 Excel 导入

需要体现以下阶段：

1. 正在读取文件。
2. 正在识别表头。
3. 正在解析题目。
4. 正在合并并保存数据。
5. 导入完成。

要求：

- 处理期间禁用文件选择和相关危险操作。
- 无法计算精确进度时使用阶段进度，不伪造百分比。
- 完成后保留结果摘要，直到用户主动关闭或再次导入。

### 11.3 题图导入

- 显示已处理文件数和总文件数。
- 完成后区分已关联、未匹配和失败。
- 单张图片失败不应中断其他图片处理。

### 11.4 本地保存

- 收藏、取消收藏和错题更新采用即时界面反馈。
- 后台保存失败时恢复或标记未保存状态。
- 连续保存期间避免重复弹出成功提示。

### 11.5 搜索

- 普通搜索延迟约 200 毫秒执行。
- 数据量较小时无需展示明显加载动画。
- 搜索耗时较长时展示结果骨架或“正在搜索”。

## 12. 错误与异常状态

### 12.1 题库导入错误

需要覆盖：

- 文件不是 `.xlsx`。
- 文件为空或无法读取。
- 找不到真实表头行。
- 缺少章节、题型、题目或正确答案等必填列。
- 部分行格式无效。
- 文件解析失败。
- 数据保存失败。

错误信息需要说明原因和修改建议，不能只显示“导入失败”。

### 12.2 考试配置错误

需要覆盖：

- 未选择章节。
- 三种题型题数均为零。
- 输入负数或非整数。
- 请求题数超过已选章节可用数量。
- 配置生成后题目数量为零。

错误优先在对应配置区域就地展示，并同步禁用开始按钮。

### 12.3 答题异常

需要覆盖：

- 题目数据缺失。
- 题图加载失败。
- 本地保存失败。
- 页面意外刷新或关闭。
- 题目对应选项或答案格式异常。

当前版本不要求恢复刷新前的答题进度，但必须在用户主动离开时给出风险提示。答题草稿恢复可作为后续版本能力。

### 12.4 本地存储异常

可能原因：

- 浏览器隐私模式限制。
- 存储空间不足。
- IndexedDB 不可用。
- 数据损坏。

处理要求：

- 尝试降级使用 localStorage。
- 明确提示哪些数据可能无法保存。
- 提供重新加载、导出当前数据或查看帮助的入口。

### 12.5 离线与依赖异常

- 已缓存核心资源时，离线不应阻断普通练习。
- 离线状态使用轻量提示，不反复打断用户。
- Excel 解析依赖不可用时，禁用导入并说明原因。
- 本地存储依赖不可用但已降级时，提示当前处于兼容模式。

## 13. 移动端适配需求

### 13.1 导航结构

- 手机端不直接横向展示六个一级导航按钮。
- 建议使用底部导航承载“练习、考试、复习、更多”。
- “复习”进入后提供错题和收藏两个入口。
- “更多”包含搜索、题库管理和主题设置。
- 答题进行中隐藏普通底部导航，使用答题专用操作栏。

### 13.2 章节练习

- 一级资料分组和章节列表采用单列。
- 章节名称允许换行。
- 题型数量在空间不足时换行显示，不能横向溢出。
- 练习配置采用底部面板。
- 数字输入调用适合的数字键盘。

### 13.3 模拟考试配置

- 章节选择在窄屏下使用单列。
- 题数输入纵向排列。
- 页面底部保留明显的考试总题数和开始按钮。
- 配置内容较长时，开始按钮可以使用底部固定操作区，但不能遮挡内容。

### 13.4 答题页

- 题干、题图和选项全部使用单列。
- 题号导航使用抽屉、折叠面板或全屏题号面板。
- 上一题、下一题和交卷固定在底部安全区域。
- 固定操作栏需要适配全面屏安全区。
- 长选项自然换行，选项字母与首行对齐。
- 多个填空输入框纵向排列。
- 软键盘弹出后保证当前输入框可见。
- 题图点击后支持全屏预览和缩放。
- 触控目标最小尺寸为 44 × 44 像素。

### 13.5 错题、收藏和搜索

- 列表均采用单列。
- 筛选和重新练习操作允许换行或放入固定工具栏。
- 题目详情展开后避免横向滚动。
- 取消收藏和移出错题操作避免紧邻主要操作。

### 13.6 题库管理

- 统计指标自动换行为两列或单列。
- 上传区域宽度占满内容区。
- 导入、导出和危险操作纵向分组。
- 危险操作按钮不与普通按钮并排。
- 章节总览采用列表，不使用需要横向滚动的表格。

### 13.7 建议断点

- 手机：小于 640px。
- 平板：640px 至 1024px。
- 桌面：大于 1024px。

平板横屏是重要刷题场景，需要重点验证题号导航、题图、选项和底部操作区域。

## 14. 可访问性要求

- 页面主要功能可以仅使用键盘完成。
- 所有输入框具有可见标签。
- 所有图标按钮具有可读名称。
- 当前导航、已选选项、正确、错误、标记等状态不能只依赖颜色。
- 焦点状态始终清晰可见。
- 对话框打开后限制焦点范围，关闭后恢复焦点。
- 动态错误和导入结果通过无障碍状态区域通知。
- 题图具有包含题目 ID 的替代文本。
- 正文、次要文本和占位文字具备足够对比度。
- 用户启用减少动态效果时，关闭非必要动画。
- 页面缩放至 200% 后仍可完成核心操作。

## 15. 数据与持久化需求

### 15.1 本地持久化内容

- 标准化题库。
- 错题记录：题目 ID、错误次数、最近答错日期。
- 收藏题目 ID。
- 主题等用户设置。
- 本地题图。
- 当前数据版本。

### 15.2 数据一致性

- 收藏状态在答题页、收藏页和搜索页保持一致。
- 错题数量在结果页、错题集和题库管理统计中保持一致。
- 题库更新后，章节数量和题型统计立即刷新。
- 删除题目后，不展示无效的错题和收藏记录。
- 主题设置在刷新后保持。

### 15.3 数据安全提示

- 在题库管理页明确说明数据保存在当前浏览器。
- 建议用户在大量导入或长期使用后定期导出完整备份。
- 清空浏览器数据可能导致题库、错题、收藏和题图丢失。
- 执行清空操作前，如存在自定义数据，可提示先导出备份。

## 16. 统计与结果计算

### 16.1 练习结果

- 总题数 = 本次试卷题目数量。
- 正确数 = 判分正确的题目数量。
- 错误数 = 答错题目数量 + 未作答题目数量。
- 正确率 = 正确数 ÷ 总题数，四舍五入显示整数百分比。

### 16.2 模拟考试结果

- 总分固定为 100 分。
- 每题分值 = 100 ÷ 总题数。
- 得分 = 正确题目数量 × 每题分值。
- 页面可显示一位小数的总得分和两位小数的每题分值。
- 单选、多选和填空题按题等权，不按题型设置不同分值。

### 16.3 错题统计

- 首次答错时错误次数为 1。
- 再次答错时累计加 1。
- 每次答错更新最近答错日期。
- 正确作答不会自动将题目移出错题集。

## 17. 原型交付要求

Stitch 原型需要覆盖以下桌面端关键画面：

1. 访问验证页。
2. 章节练习正常状态。
3. 章节练习空状态。
4. 练习配置弹层及范围错误状态。
5. 模拟考试配置正常状态及配置错误状态。
6. 单选题答题页。
7. 多选题答题页。
8. 多空填空题答题页。
9. 展开的题号导航。
10. 交卷确认。
11. 有错题的练习结果页。
12. 全部答对的结果页。
13. 模拟考试结果页。
14. 错题集正常、筛选无结果和完全为空状态。
15. 收藏集正常和空状态。
16. 搜索初始、搜索有结果和搜索无结果状态。
17. 题库管理正常状态。
18. Excel 导入处理中、成功和失败状态。
19. 清空数据确认对话框。
20. 本地存储或离线异常提示。

移动端原型至少覆盖：

1. 移动端一级导航。
2. 章节练习页。
3. 练习配置底部面板。
4. 单选题答题页。
5. 填空题与软键盘场景。
6. 题号导航抽屉。
7. 交卷确认。
8. 答题结果页。
9. 错题集页。
10. 题库管理页。

原型中需要明确表达：

- 点击区域。
- 页面跳转方向。
- 弹层打开与关闭方式。
- 表单错误位置。
- 按钮禁用和处理中状态。
- 列表空状态。
- 题号的当前、已答、未答和标记状态。
- 桌面端与移动端的结构差异。

## 18. 功能优先级

### P0：本期必须完成

- 访问验证。
- 一级导航。
- 章节分组与章节练习配置。
- 单选、多选和填空题答题流程。
- 题号导航、标记、收藏、翻页和交卷。
- 模拟考试配置与百分制结果。
- 答题结果和错题详情。
- 错题集与错题重练。
- 收藏集与收藏重练。
- 题目搜索。
- Excel 导入、题图关联、数据导出和清空操作。
- 所有主要空状态、加载状态和错误状态。
- 手机、平板和桌面适配。
- 本地数据保存与离线使用提示。

### P1：建议随重构完成

- 错题排序。
- 移除错题和取消收藏后的撤销。
- 题图全屏预览。
- 搜索结果分批渲染。
- 导入阶段进度。
- 离开答题流程的风险提示。
- 更完整的键盘操作。

### P2：后续版本

- 自动保存并恢复未完成答题。
- 最近练习记录。
- 学习趋势和章节正确率统计。
- 用户自定义单选自动跳题开关。
- 更丰富的搜索筛选。
- 错题掌握状态和复习计划。
- 跨设备云同步。

## 19. 验收标准

### 19.1 核心流程

- 用户可以从章节列表选择章节和合法范围并开始练习。
- 用户可以完成单选、多选和多空填空题。
- 返回上一题时已填写答案保持不变。
- 用户可以通过题号导航访问任意题目。
- 交卷前可以看到未答题数量。
- 交卷后结果统计与实际答案一致。
- 错题被正确加入错题集并累计错误次数。
- 收藏状态在所有相关页面保持一致。

### 19.2 模拟考试

- 用户可以选择多个章节。
- 可用题量随章节选择实时变化。
- 不合法配置不能开始考试。
- 生成题目数量符合配置。
- 模拟考试得分按 100 分等权计算。

### 19.3 题库管理

- 用户可以选择或拖入合法 Excel 文件。
- 导入结束后显示新增、更新、重复和无效数量。
- 非法文件显示可理解的错误原因。
- 题图可以按题目 ID 正确关联。
- 完整备份包含题库、错题、收藏、设置和题图。
- 清空操作必须经过明确确认。
- 存在内置题库时，清空自定义题库不会删除内置题库。

### 19.4 状态完整性

- 所有主要列表均有完整空状态。
- 应用启动、文件导入和题图处理均有加载或处理中状态。
- 表单错误在对应位置显示。
- 本地保存或依赖失败时用户能够知道发生了什么。
- 关键异步操作不能被重复提交。

### 19.5 响应式与可访问性

- 320px 宽度下不存在页面级横向滚动。
- 手机端一级导航不会拥挤或被截断。
- 手机端答题操作不会被安全区或软键盘遮挡。
- 平板横屏可以同时清楚查看题目、题图、选项和进度。
- 所有主要触控目标不小于 44 × 44 像素。
- 键盘用户可以完成核心答题流程。
- 状态信息不只依赖颜色表达。

## 20. 待确认事项

以下问题不阻塞原型第一版，但在进入开发前需要确认：

1. 移动端一级导航最终采用底部导航还是顶部菜单。
2. 单选题是否继续默认自动跳到下一题，或增加用户开关。
3. 答题中刷新页面后是否需要恢复未完成进度。
4. 错题重练完成且答对后，是否自动移出错题集。
5. 收藏和错题列表是否需要支持批量选择与批量移除。
6. 是否需要在题库管理页提供现有 Excel 模板的直接下载入口。
7. 访问验证是否继续仅作为本设备前端验证，还是后续接入正式身份验证。
8. 搜索结果是否需要分页，以及单页最大展示数量。
9. 完整备份是否需要提供导入恢复入口；当前项目主要具备导出能力。
10. 默认题库与用户自定义题库是否需要在界面中明确区分。

---

本 PRD 以当前项目已有能力为边界，主要用于指导前端功能模块重构和 Stitch 原型设计。视觉风格、品牌规范、颜色、字体和具体组件外观不在本文档中限定，可在原型设计阶段另行确定。
